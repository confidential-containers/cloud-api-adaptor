apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
  namespace: kube-system
spec:
  storageClassName: ibmc-vpc-block-5iops-tier
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
