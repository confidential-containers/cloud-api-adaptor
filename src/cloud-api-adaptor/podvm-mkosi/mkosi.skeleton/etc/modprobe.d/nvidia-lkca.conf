# Linux Kernel Crypto API (LKCA) — required for secure GPU-driver communication
# in confidential compute mode. The ecdsa_generic and ecdh modules must be loaded
# before the nvidia module to establish the encrypted channel.
install nvidia /sbin/modprobe ecdsa_generic; /sbin/modprobe ecdh; /sbin/modprobe --ignore-install nvidia
