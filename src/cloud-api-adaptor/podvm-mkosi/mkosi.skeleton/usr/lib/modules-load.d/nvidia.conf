# Load NVIDIA kernel modules on boot so nvidia-persistenced and nvidia-cdi
# can start without waiting for DKMS to compile and modprobe manually.
# DKMS builds the modules but does not load them; systemd-modules-load.service
# reads this file and inserts the modules early in boot.
nvidia
nvidia-uvm
nvidia-modeset
