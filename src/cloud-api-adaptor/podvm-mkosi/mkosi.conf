# This file is kept for reference only.
# The actual configuration files used are:
#   - mkosi.conf.fedora (for Fedora builds)
#   - mkosi.conf.ubuntu (for Ubuntu builds)
#
# The Dockerfiles copy the appropriate distribution-specific config
# to mkosi.conf at build time.
#
# If you need to modify the base configuration, update BOTH:
#   - mkosi.conf.fedora
#   - mkosi.conf.ubuntu

[Content]
Environment=SOURCE_DATE_EPOCH=0
RemoveFiles=/var/log
RemoveFiles=/var/cache

[Output]
OutputDirectory=build

[Distribution]
# Distribution is set in mkosi.conf.fedora or mkosi.conf.ubuntu
