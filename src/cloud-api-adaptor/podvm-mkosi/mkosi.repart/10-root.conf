[Partition]
Type=root
Format=ext4

# Fixed size: OS only uses ~688M, so 4G is plenty with headroom for logs/temp files
# This maximizes space for sda3 (scratch/data partition) which gets ~244G
SizeMinBytes=4G
