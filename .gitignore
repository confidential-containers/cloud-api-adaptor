#Directories
install/runtime-payload/bin
pause_bundle
output
staticlib
pause
umoci
skopeo

# Binaries
agent-protocol-forwarder
attestation-agent
# The code files under cmd/agent-protocol-forwarder are not ignored
!/cmd/agent-protocol-forwarder
cloud-api-adaptor
# The code files under cmd/cloud-api-adaptor are not ignored
!/cmd/cloud-api-adaptor
kata-agent
cluster-provisioner

# Ignore the contents of the certificates directory
podvm/files/etc/certificates/*

# IDEs
.vscode

# Files
vcenter.auto.pkrvars.hcl
settings.auto.pkrvars.hcl
*.qcow2
.git-commit
.git-commit.tmp
